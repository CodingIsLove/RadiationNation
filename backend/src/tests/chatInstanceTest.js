let assert = require('assert');
let {describe,it} = require('mocha');

describe('ChatInstanceRoutes',()=>{
  describe('REQNAME',()=>{
    it('REQ DESC', ()=>{
     assert.equal([1,2,3].indexOf(4),-1);
    })
  })
});

describe('GameInstanceRoutes',()=>{
  describe('REQNAME',()=>{
    it('REQ DESC', ()=>{
     assert.equal([1,2,3].indexOf(4),-1);
    })
  })
});

describe('ResourceRoutes',()=>{
  describe('REQNAME',()=>{
    it('REQ DESC', ()=>{
     assert.equal([1,2,3].indexOf(4),-1);
    })
  })
});

describe('UserRoutes',()=>{
  describe('REQNAME',()=>{
    it('REQ DESC', ()=>{
     assert.equal([1,2,3].indexOf(4),-1);
    })
  })
});
